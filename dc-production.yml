version: '3'
services:
  db:
    restart: always

  app:
    restart: always

  nginx:
    restart: always

  composer:
    command: "composer install --no-dev"

  cloudStorage:
    restart: always


